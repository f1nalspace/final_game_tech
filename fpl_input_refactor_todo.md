# FPL Input Refactor — Progress TODO

Companion to `fpl_input_proposal_plan.md`. Tracks step-by-step status so a future session can resume cleanly.

Branch: `input-refactoring`
Last verified: builds clean on Linux/POSIX (FPL_Console, FPL_Window, FPL_OpenGL, FPL_Test). FPL_Input verified end-to-end on X11 with a Logitech F310 gamepad — connection + axis/button events flow correctly. Win32 path not re-verified after step 6/7 — needs a Windows build.

## User-Updates

_None pending. (Last applied: step 9c — implement input device enumeration API.)_

## Done

- [x] **Step 1 — Public types + `FPL_NO_INPUT`.** Commit `243b6ce6` ("Introduced a new input API"). Adds `fplInputSourceType`, `fplInputBackendType`, `fplInputBackendMask` (+ inline mask helpers), extended `fplInputSettings`, `fplInputDevice` family, `fplInputBackendSupport`, new public-API declarations + stub impls, extended `fplMouseButtonType` with X1/X2 + `fplMouseState.wheelDeltaY/X`, `FPL_NO_INPUT` + `FPL_NO_INPUT_*` per-backend switches, `FPL__SUPPORT_INPUT*` / `FPL__ENABLE_INPUT*`. Wayland scrubbed from code and plan.
- [x] **Step 2 — Skeleton stubs.** Commit `da7c623d` ("Input system stubs and bare-bone initialization"). Adds `fpl__InputContext`, `fpl__NativeInputEvent[Kind]`, `fpl__InputSystem_Init/Release/Update/HandleNativeEvent/IsEnabled/PollKeyboard/PollMouse/PollGamepad` stubs, wires init after audio and release before audio in `fplPlatformInit` / `fpl__ReleasePlatformStates`. Also reworked init flags: per-source `fplInitFlags_Keyboard`, `fplInitFlags_Mouse`, kept `fplInitFlags_GameController`; `fplInitFlags_Input` is now a composite alias = Keyboard | Mouse | GameController.
- [x] **Step 3 — XInput migration.** Commit `2c0de90b` ("Migrated XInput to new input system"). XInput types moved from `FPL__ENABLE_WINDOW` to `FPL__ENABLE_INPUT_XINPUT`. Renamed `fpl__Win32XInputState` → `fpl__InputBackendXInput` (with `isInitialized`, `api`). Removed `xinput` field from `fpl__Win32AppState`; added it to `fpl__InputContext`. New backend wrappers `fpl__InputBackendXInput_Init/Release/Update/PollGamepad`. `fpl__InputSystem_*` now dispatch to XInput. `fpl__Win32InitPlatform` / `fpl__Win32ReleasePlatform` no longer touch XInput. `fpl__Win32UpdateGameControllers` removed; Win32 `fplWindowUpdate` calls `fpl__InputSystem_Update`. Win32 `fplPollGamepadStates` delegates to `fpl__InputSystem_PollGamepad`. **Temporary:** `FPL__ENABLE_INPUT_XINPUT` requires `FPL__SUPPORT_WINDOW` for now — step 8 lifts this once gamepad event helpers are window-independent.
- [x] **Step 4 — Win32 keyboard/mouse migration.** New `fpl__InputBackendWin32` (in `fpl__InputContext.win32kbm`) with `Init/Release/PollKeyboard/PollMouse/HandleNativeEvent`. Win32 `fplPollKeyboardState` / `fplPollMouseState` delegate to `fpl__InputSystem_PollKeyboard` / `_PollMouse`. WndProc forwards `WM_SYSKEYDOWN/UP`, `WM_KEYDOWN/UP`, `WM_CHAR/SYSCHAR/UNICHAR/DEADCHAR`, `WM_*BUTTON*` (incl. X1/X2), `WM_MOUSEMOVE`, `WM_MOUSEWHEEL`, `WM_MOUSEHWHEEL` via `fpl__InputSystem_HandleNativeEvent` (payload = stack-allocated MSG). Backend handler does the original button/move/wheel work plus `SetCapture`/`ReleaseCapture` (both gated on `disabledEvents` and `enabledSources`). Back-compat tweak in `fpl__PlatformInit`: `fplInitFlags_Window` implies Keyboard+Mouse sources (Gamepad still requires explicit flag) so `FPL_Window` / `FPL_OpenGL` keep getting input. `FPL__ENABLE_INPUT_WIN32` gated on `FPL__SUPPORT_WINDOW` for now (lifted by step 8). `WM_MOUSEHWHEEL` is forwarded but discarded internally (no horizontal axis on `fpl__HandleMouseWheelEvent` yet — step 9). Linux build clean (FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test / FPL_Input). **Win32 verified working** — needed forward declarations for `fpl__InputSystem_*` after the `fpl__InputContext` typedef (Win32 wndproc uses them ~15k lines before their definition at ~31k).
- [x] **Step 5 — X11Kbm migration.** New `fpl__InputBackendX11Kbm` (in `fpl__InputContext.x11kbm`, struct lives in `TYPES_X11`) with `Init/Release/PollKeyboard/PollMouse/HandleNativeEvent`. X11 `fplPollKeyboardState` / `fplPollMouseState` delegate to `fpl__InputSystem_PollKeyboard` / `_PollMouse`. `fpl__X11HandleEvent` collapses `KeyPress/KeyRelease/ButtonPress/ButtonRelease/MotionNotify` into a single forwarder that builds an `fpl__NativeInputEvent{X11Event, ev}` and calls `fpl__InputSystem_HandleNativeEvent`. Backend handler keeps the original `XEventsQueued`/`XPeekEvent` repeat-detection trick on `KeyRelease`. `FocusIn/FocusOut`, `EnterNotify`, `LeaveNotify` stay in window code (focus events aren't input events; enter/leave were never implemented and remain a separate task). `FPL__ENABLE_INPUT_X11` gated on `FPL__SUPPORT_WINDOW` for now (lifted by step 8). Linux build clean (FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test / FPL_Input).
- [x] **Step 6 — Linux joystick migration (lift as-is).** New `fpl__InputBackendLinuxJoystick` (in `fpl__InputContext.linuxJoystick`, struct lives in `TYPES_LINUX` under `FPL__ENABLE_INPUT_LINUX_JOYSTICK`) owns the `fpl__LinuxGameController controllers[FPL__LINUX_MAX_GAME_CONTROLLER_COUNT]` array + `lastCheckTime`. Wrappers `Init/Release/Update/PollGamepad`. Detection/state helper renamed `fpl__LinuxJoystick_PollImpl(settings, backend, useEvents)` — same body, just operates on backend. `fpl__LinuxFreeGameControllers` folded into `_Release`. X11 `fplWindowUpdate` no longer calls the joystick directly; it calls `fpl__InputSystem_Update` (matching Win32). Linux `fplPollGamepadStates` delegates to `fpl__InputSystem_PollGamepad`. `fpl__LinuxReleasePlatform` cleared (release happens via `fpl__InputSystem_Release` before audio). Dropped `controllersState` field from `fpl__LinuxAppState`. **Temporary:** `FPL__ENABLE_INPUT_LINUX_JOYSTICK` gated on `FPL__SUPPORT_WINDOW` for now — step 8 lifts this once gamepad event helpers are window-independent. Linux build clean (FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test).
- [x] **Step 7 — Linux joystick auto-detection + throttling (no udev).** Scan `/dev/input/js0..js31` from `fpl__LinuxJoystick_DetectControllers` (called by `_Update`), throttled by `gameControllers.detectionFrequency`. `_PollGamepad` no longer runs detection — mirrors XInput, just refreshes cached state via `fpl__LinuxJoystick_UpdateStates(false)` and copies. Added `fpl__InputBackendLinuxJoystick.triedSlot[FPL__LINUX_JOYSTICK_SCAN_COUNT]` for one-shot `FPL_LOG_DEBUG` per slot on (a) non-`ENOENT` open failures, (b) zero axes/buttons (virtual nodes), (c) missing init message. `ENOENT` skipped silently. Added `fpl__LinuxGameController.slotIndex` so `ENODEV` in read path can reset `triedSlot[slotIndex]` for hotplug re-announce. Linux build clean (FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test).
- [x] **Bugfix — Linux joystick init-message guard.** Commit `aeb0a3a3`. Pre-existing bug exposed by step 7's scan-walking past virtual js0/js1 to the real F310 on js2: kernel ORs `JS_EVENT_INIT` (0x80) onto `JS_EVENT_AXIS`/`JS_EVENT_BUTTON` for the queued initial events, so `msg.type` is 0x81/0x82 — the strict equality `msg.type == JS_EVENT_AXIS` etc. never matched and every real gamepad failed detection. Fix masks `JS_EVENT_INIT` off before comparing in `fpl__LinuxJoystick_DetectControllers`. Verified with FPL_Input + Logitech F310.
- [x] **Step 8 — Event plumbing.** Gamepad connect/disconnect/state events now flow through unified helpers `fpl__PushGamepadConnectionEvent` / `fpl__PushGamepadStateEvent` from every backend. XInput already used them since step 3; Linux joystick was still building `fplEvent` inline in `fpl__LinuxJoystick_DetectControllers` and `fpl__LinuxJoystick_UpdateStates` and calling `fpl__PushInternalEvent` directly. Replaced both connect (one site) and disconnect/state-change (two sites) with the helpers. Behavior is identical — connection helper additionally sets `state.isConnected` + `state.deviceName`, which was previously zero/null in the connect path. Backends drive events through `fpl__InputSystem_Update` (called from `fplWindowUpdate` on Win32 and X11); window code does not push gamepad events anywhere. `fplPollEvents` consumers still see `fplGamepadEvent` correctly because the queue and helpers are unchanged. Linux build clean (FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test / FPL_Input).
- [x] **Step 9b (partial) — Plumbing for `FPL_NO_WINDOW` keyboard/mouse polling.** Lifted `FPL__SUPPORT_WINDOW` gate from `FPL__ENABLE_INPUT_WIN32` and `FPL__ENABLE_INPUT_X11`. `FPL_SUBPLATFORM_X11` now stays alive when `FPL_NO_WINDOW` if `FPL__SUPPORT_INPUT_X11` is set (new gate placed after the input support block, which is the first point both flags are decided). `fpl__InputSystem_PollKeyboard` / `_PollMouse` lifted out of `FPL__ENABLE_WINDOW`. Win32 `fplPollMouseState` extracted from the second `FPL__ENABLE_WINDOW` block (placed alongside `fplPollKeyboardState` + `fplPollGamepadStates` in the gap between window blocks). X11 `fplPollKeyboardState` / `_PollMouse` already live inside `FPL_SUBPLATFORM_X11` (now no-window-friendly); window-only X11 helpers (`fpl__X11ReleaseWindow`, `fpl__X11LoadWindowIcon`..`fpl__X11InitWindow`..`fpl__X11HandleTextInputEvent`, `fpl__X11HandleEvent`, all `fpl*Window*` platform_api up to `fplSetClipboardText`) now wrapped in `#if defined(FPL__ENABLE_WINDOW)`. The X11 input backend's `fpl__InputBackendX11Kbm_HandleNativeEvent` is gated on `FPL__ENABLE_WINDOW`; the Win32 backend's `_HandleNativeEvent` is now also gated on `FPL__ENABLE_WINDOW` (it relies on `fpl__Handle*Event` helpers + `appState->window` which only exist there). `fpl__InputSystem_HandleNativeEvent` body wrapped in `FPL__ENABLE_WINDOW`. Win32 helpers `fpl__Win32IsKeyDown`, `fpl__Win32IsKeyActive`, `fpl__Win32GetKeyboardModifiers` re-gated to `FPL__ENABLE_WINDOW || FPL__ENABLE_INPUT_WIN32` (previously window-only) so `_PollKeyboard` / `_PollMouse` can use them in detached mode without dragging in window code. Backends (`fpl__InputBackendX11Kbm`, `fpl__InputBackendWin32`) gained a `detached` flag and runtime detection (`(initFlags & fplInitFlags_Window) == 0` OR `fplInputSettings.detachFromWindow`). X11 backend in detached mode: opens its own `Display*` via `XOpenDisplay(NULL)`, builds a `detachedKeyMap[256]` via `XGetKeyboardMapping`, queries pointer against root window. Win32 backend in detached mode: registers `FPLInputMessageWindow` class + creates `HWND_MESSAGE` (placeholder for future `WM_INPUT`/`WM_DEVICECHANGE` plumbing), `_PollMouse` skips `ScreenToClient` (returns screen coords), `_PollKeyboard` skips `buttonStatesMapped`. Linux build clean for FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test / FPL_Input / FPL_Crackout / FPL_ConsoleGamepad (FPL_NO_WINDOW). Win32 verified by user — earlier attempt on MSVC failed due to ungated callers; resolved with the helper re-gating noted above. **Pending in Step 9b:** (a) extend / add a new demo to actually exercise no-window keyboard+mouse polling (FPL_ConsoleGamepad currently only polls gamepad), (b) wire `fpl__InputSystem_Update` into `fplPollEvent`/`fplPollEvents` (or a new pump) so event-mode keyboard/mouse can flow without `fplWindowUpdate`, including draining the Win32 `HWND_MESSAGE` and registering for `WM_INPUT`/`WM_DEVICECHANGE`.

- [x] **Step 9b (demo) — FPL_ConsoleGamepad polls keyboard + mouse.** Commit `2abbc90b`. `demos/FPL_ConsoleGamepad/fpl_console_gamepad.c` now inits with `fplInitFlags_Input` (Keyboard | Mouse | GameController) and once per second calls `fplPollKeyboardState` / `fplPollMouseState` alongside `fplPollGamepadStates`. Prints active modifiers + held raw key indices, cursor position + held mouse buttons + accumulated wheel deltas, and per-controller stick/trigger/button info. Verified on Linux/X11 with `DISPLAY=:0` + Logitech F310 (js1): mouse pos updates from XQueryPointer against the root window, gamepad connection/state events flow, keyboard polling returns no-held-keys when no key is held (XQueryKeymap on the detached Display). Confirms the detached X11 backend (`fpl__InputBackendX11Kbm` with `ownsDisplay=true`) actually works end-to-end. Win32 screen-coordinate mouse fallback not re-verified after the demo change — needs a Windows run. Event-flow part of step 9b split out as a separate pending step.
- [x] **Step 9b (no-window pump) — `fplPollEvent` / `fplPollEvents` without window.** Commit `4f03843d`. Relocated the keyboard/mouse `fpl__Push*Event` primitives (`fpl__PushKeyboardButtonEvent`, `_PushKeyboardInputEvent`, `_PushMouseButtonEvent`, `_PushMouseWheelEvent`, `_PushMouseMoveEvent`) out of the window-only block (`#if defined(FPL__ENABLE_WINDOW)`) into the `WINDOW || INPUT` block above (matching the `fpl__PushGamepad*Event` helpers from step 8). The window-only `fpl__Handle*Event` wrappers + `fpl__GetMappedKey` stay in the window block since they need `windowState->keyMap` / `windowState->keyStates`. Added `fpl_common_api fplPollEvent` / `fplPollEvents` fallbacks under `#if !defined(FPL__ENABLE_WINDOW)` near `fplUpdateInputDevices` — they drive `fpl__InputSystem_Update` and drain the internal event queue. Window-mode platform impls untouched. Linux build clean for FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test / FPL_Input / FPL_Crackout / FPL_ConsoleGamepad.
- [x] **Step 9c — Input device enumeration API.** Replaced the four stubs in `final_platform_layer.h` (line ~13805 block) with real implementations. Added `fpl__MakeInputDeviceGuid(backend, source, slot)` (deterministic 16-byte GUID using ASCII prefix `FPLI` + backend/source/slot bytes), `fpl__InputBackendDescriptor` static descriptor struct, `fpl__BuildInputBackendDescriptors` (emits descriptors only for compiled-in backends — XInput/Win32/X11Kbm/LinuxJoystick), and `fpl__EnumerateInputDevices(sourceFilter, out, maxDevices)`. `fplGetInputBackendSupport` returns the descriptor count when `out`/`maxCount` is null/0, else writes up to `maxCount` and returns the written count. `fplGetInputBackendSupportByType` linear-scans the descriptor table. `fplGetInputDevices` walks `fpl__InputContext`: Win32/X11Kbm contribute one keyboard + one mouse when initialized (`Connected` always); XInput contributes one entry per `xinput.isConnected[i]` slot; LinuxJoystick one entry per `controllers[i].fd > 0` slot (uses `displayName` if set, else `deviceName`, and copies cached `fplGamepadState` into `dev->state.gamepad`). `fplFindInputDevice` enumerates into a stack-local `FPL_MAX_INPUT_DEVICE_COUNT` array and matches GUID via `fpl__IsEqualsMemory`. Existing `fplGetKeyboardDevices` / `fplGetMouseDevices` / `fplGetGamepadDevices` shims keep working. Build clean for FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test / FPL_Input / FPL_Crackout / FPL_ConsoleGamepad.
- [x] **Step 9a — Lift `FPL__SUPPORT_WINDOW` gate from gamepad backends.** Macros: `FPL__ENABLE_INPUT_XINPUT` and `FPL__ENABLE_INPUT_LINUX_JOYSTICK` no longer require `FPL__SUPPORT_WINDOW`. Public types block (line ~7573) gated on `FPL__ENABLE_WINDOW || FPL__ENABLE_INPUT` so `fplEvent`, `fplGamepadState[s]`, `fplGamepadEvent[Type]`, `fplPollGamepadStates` declaration etc. are visible without window. Internal: `fpl__EventQueue` typedef + `fpl__PushInternalEvent` / `_PollInternalEvent` / `_HasInternalEvents` / `_ClearInternalEvents` + `fpl__PushGamepadConnectionEvent` / `_StateEvent` moved out of the window-only block into a new `FPL__ENABLE_WINDOW || FPL__ENABLE_INPUT` block. `eventQueue` field moved from `fpl__PlatformWindowState` to `fpl__PlatformAppState` (queue is shared, not window-owned). `fpl__InputSystem_PollGamepad` (forward decl + impl) lifted out of `FPL__ENABLE_WINDOW`. Win32 `fplPollGamepadStates` extracted from the giant `FPL__ENABLE_WINDOW` block (closes WINDOW briefly, defines fn under `FPL__ENABLE_INPUT`, reopens WINDOW). Linux `fplPollGamepadStates` block re-gated to `FPL__ENABLE_INPUT`. Input stub group (`fplGetInputBackendSupport`, `fplGet*Devices`, `fplFindInputDevice`, `fplUpdateInputDevices`) moved from `FPL__ENABLE_WINDOW` to `FPL__ENABLE_INPUT`; `fplUpdateInputDevices()` now actually calls `fpl__InputSystem_Update` (was a stub returning false) so console-only apps drive detection + state events from outside the window pump. New demo: `demos/FPL_ConsoleGamepad/` (`FPL_NO_WINDOW`/`FPL_NO_VIDEO`/`FPL_NO_AUDIO`, `fplInitFlags_GameController` only) — polls gamepad in a 16 ms loop via `fplUpdateInputDevices` + `fplPollGamepadStates`, prints stick/trigger + held buttons once a second + connect/disconnect transitions. Linux build clean (FPL_Console / FPL_Window / FPL_OpenGL / FPL_Test / FPL_Input / FPL_Crackout / FPL_ConsoleGamepad). Step 9b still pending — `FPL__ENABLE_INPUT_WIN32` / `_X11` (kbm backends) still require `FPL__SUPPORT_WINDOW`; lifting that needs the hidden `HWND_MESSAGE` window + detached X11 `Display*` + `fplInputSettings.detachFromWindow` plumbing.

## In progress

_None._

## Pending
- [ ] **Step 9b (Win32 RawInput) — Detached HWND_MESSAGE event source.** No-window pump landed, but neither kbm backend currently pushes events in detached mode — so `fplPollEvent` returns nothing but gamepad events on Win32/X11 detached. Win32 work to do: (1) Add `GetRawInputData` to the `fpl__Win32UserApi` loader (currently only `RegisterRawInputDevices` is loaded, used by the cursor-hide path at ~14486). (2) Replace the `DefWindowProcW` placeholder in `fpl__InputBackendWin32_Init` with a custom WndProc that handles `WM_INPUT` (translate RAWMOUSE / RAWKEYBOARD into `fpl__PushMouse*Event` / `fpl__PushKeyboardButtonEvent`) and `WM_DEVICECHANGE` (trigger gamepad rescan). Use `SetWindowLongPtrW(GWLP_USERDATA, backend)` to plumb the backend pointer to the WndProc. (3) After `CreateWindowExW`, call `RegisterRawInputDevices` with `{HID_USAGE_PAGE_GENERIC, HID_USAGE_GENERIC_MOUSE, RIDEV_INPUTSINK, hwnd}` and the keyboard equivalent (usage 6). (4) Add `fpl__InputBackendWin32_Update` that `PeekMessageW` + `DispatchMessageW` on the message window. (5) Wire it into `fpl__InputSystem_Update` (alongside the existing XInput / LinuxJoystick updates). (6) Add a `wheelDeltaX/Y` accumulator to `fpl__InputBackendWin32` so `_PollMouse` returns and clears it. Untestable on this Linux box — user runs the Windows toolchain to verify. **Mouse wheel only works once this lands.** X11 detached event path stays polling-only; lifting that would need a hidden `InputOnly` window with `ButtonPressMask` and is deferred (covers wheel + X1/X2 if anyone ever needs it).
- [x] **Step 10 — Multi-backend gamepad polling.** `fpl__InputSystem_PollGamepad` now clears `outStates` once before iterating backends. Each backend writes only into slots it owns AND that are still unclaimed (`isConnected==false`); first-backend-wins for index conflicts. XInput: removed its top-of-fn `fplClearStruct(outStates)`, added a skip-if-claimed guard, sets `targetPadState->isConnected = true` (XInput populated all fields except `isConnected` previously, which is exactly the field the merge contract uses to decide ownership). LinuxJoystick: replaced unconditional copy-all with skip-empty (`fd <= 0`) + skip-claimed loop, and now returns `false` when nothing was filled instead of unconditional `true`. Smoke test added to `FPL_Test`: pre-fills every slot with garbage + `isConnected=true`, calls `fplPollGamepadStates`, asserts every slot reported as disconnected has been zeroed (i.e. system clear ran). Verified: FPL_Test runs end-to-end with no assert failures on Linux. Fake-second-backend test deferred — would require either exposing backend registration or compile-time test backend; revisit alongside `fplInputBackendType_Custom` (open question §12).
- [ ] **Step 11 — Linux udev hotplug backend.** Add `fpl__InputBackendLinuxUdev` that opens a udev netlink socket, listens for `add`/`remove` events on the `input` subsystem, and pushes connect/disconnect signals into the `LinuxJoystick` (and later `LinuxEvdev`) backend so it can rebuild its slot list without polling. Falls back gracefully to step 7's polling scan when libudev is missing or `FPL_NO_INPUT_LINUX_UDEV` is set. Runtime-link `libudev.so.1` (no build-time dep). Ensures gamepads plugged in mid-session show up immediately, not after `detectionFrequency`.
- [ ] **Step 12 — New backends + docs.** Add `LinuxEvdev`, `RawInput`, `DInput`, `UnixGamepad` as descriptors + impl structs + function-table entries. Update `page_category_input_config` docs and changelog. (Plan §13 / §11 step 11.)

## Open questions parked from §12 of the plan

- `fplInputDevice.state` union: keep as union or expose pointers/arrays?
- Add a `fplInputBackendType_Custom` slot for user-supplied backends (matches `fplAudioBackendType_Custom`)?
- Expose `fplUpdateInputDevices()` publicly (already declared as a stub) or fold into `fplPollEvents`?

## Cross-platform validation checklist (run before/after each step)

- POSIX/Linux build smoke (object files only, link not required for header coverage):
  - `gcc -std=c99 -O2 -c -I. demos/FPL_Console/fpl_console.c`
  - `gcc -std=c99 -O2 -c -I. demos/FPL_Window/fpl_window.c`
  - `gcc -std=c99 -O2 -c -I. -Idemos/additions demos/FPL_OpenGL/fpl_opengl.c`
  - `g++ -std=c++11 -O2 -c -I. -Idemos/additions demos/FPL_Test/fpl_test.cpp`
  - `g++ -std=c++11 -O2 -c -I. -Idemos/additions -Idemos/dependencies demos/FPL_Input/fpl_input.cpp`
  - `g++ -std=c++11 -O2 -c -I. -Idemos/additions -Idemos/dependencies demos/FPL_Crackout/fpl_crackout.cpp` (uses ~everything in FPL — broadest single header coverage)
  - `gcc -std=c99 -O2 -c -I. demos/FPL_ConsoleGamepad/fpl_console_gamepad.c` (canonical FPL_NO_WINDOW gamepad smoke test)
- Windows: not buildable from this dev box (no mingw). Needs the user's Windows toolchain. FPL_Input demo is the canonical gamepad/keyboard/mouse smoke test.
- After step 5/6: run X11 demos (FPL_Window, FPL_OpenGL, FPL_Input).
- After step 9a: run FPL_ConsoleGamepad with a real gamepad to verify polling works without a window.

## Resume instructions for the next session

1. `git checkout input-refactoring` (already there).
2. Re-read `fpl_input_proposal_plan.md` and this file.
3. Pick the next pending step. Mark it in_progress, implement, build-check on Linux at minimum, commit, mark done.
4. Each step should be one commit (per plan §11).

## Latest state (after step 9b no-window pump)

- Recent commits: `2abbc90b` (demo), `4f03843d` (no-window pump), preceded by step 9c (input device API).
- `fplPollEvent` / `fplPollEvents` now compile and run without a window: drain the internal queue + drive `fpl__InputSystem_Update`. With FPL_NO_WINDOW the only events that flow are gamepad connect/disconnect/state — kbm backends still need RawInput plumbing (Win32) or a hidden InputOnly window (X11) to push events. Polled state for keyboard + cursor + buttons works (verified Linux); polled wheel deltas remain zero until the event-flow lands.
- Next pending: Step 9b Win32 RawInput plumbing — see Pending section. Untestable here; user verifies on Windows.

## Known incidental issues found while testing

- `fplInputBackendMaskIsEnabled` / `_Enable` / `_Disable` are declared `fpl_inline` (= `inline`). With C99 `-O0`, GCC emits no out-of-line definition, producing `undefined reference` errors when linked from a single-translation-unit demo. Workaround: compile with `-O2`. Proper fix later: change `fpl_inline` to `static inline` on these helpers, or provide an extern definition. Not blocking step 8.
